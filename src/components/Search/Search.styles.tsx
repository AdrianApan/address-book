import { styled } from '@mui/system'
import Card from '@mui/material/Card'

const StyledCard = styled(Card)({
  padding: 8,
  marginBottom: 12,
  display: 'flex',
  alignItems: 'center',
  width: '100%',
})
export { StyledCard }
